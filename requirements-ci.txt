# CI-specific requirements (minimal for testing only)
# Core dependencies
pandas>=1.5.0
numpy>=1.21.0
python-dotenv>=0.19.0

# Alpaca trading (legacy API only for CI)
alpaca-trade-api==3.1.1

# Testing dependencies
pytest>=7.0.0
pytest-cov>=4.0.0

# Basic utilities
requests>=2.28.0
pytz>=2022.0

# Code quality tools (needed for CI/CD)
black>=23.0.0
flake8>=6.0.0
isort>=5.12.0
mypy>=1.0.0
safety>=2.3.0
bandit>=1.7.0

# Note: alpaca-py, websockets, aiohttp excluded for CI compatibility
# talib-binary excluded as it causes build issues in CI environment
# These should be installed in production/development environments separately
