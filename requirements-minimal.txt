# Paper Trading Bot - Minimal CI Dependencies
# Core data processing
pandas>=1.5.0
numpy>=1.21.0

# Paper trading API support (no real money involved)
requests>=2.28.0
python-dotenv>=0.19.0

# Testing framework
pytest>=7.0.0
pytest-cov>=4.0.0

# Code quality tools (essential for clean code)
black>=22.0.0
isort>=5.10.0
flake8>=5.0.0

# Note: alpaca-trade-api and talib-binary excluded for CI stability
# These are only needed for local development with paper trading
